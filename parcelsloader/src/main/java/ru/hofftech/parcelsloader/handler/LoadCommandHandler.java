package ru.hofftech.parcelsloader.handler;

import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;

@Slf4j
@RequiredArgsConstructor
public class LoadCommandHandler {

    //private final LoadCommandParser loadCommandParser;
    //private final ParcelRepository parcelRepository;

    public String executeCommand(String commandText) {
        //LoadTrucksCommand command = loadCommandParser.parse(commandText);
        return "";
    }
}
